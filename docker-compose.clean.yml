services:
  fshare-arr-bridge:
    build: .
    image: fshare-arr-bridge:latest
    container_name: fshare-arr-bridge
    restart: unless-stopped
    ports:
      - "8484:8484"
    env_file:
      - /bulk-storage/appdata/fshare-arr-bridge/.env
    environment:
      INDEXER_PORT: 8484
    networks:
      - fshare-network
    depends_on:
      - pyload
    healthcheck:
      test: [ "CMD", "python", "-c", "import requests; requests.get('http://localhost:8484/health')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  pyload:
    image: lscr.io/linuxserver/pyload-ng:latest
    container_name: fshare-pyload
    restart: unless-stopped
    ports:
      - "8100:8000"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - /bulk-storage/appdata/fshare-arr-bridge/pyload/config:/config
      - /bulk-storage/appdata/fshare-arr-bridge/pyload/downloads:/downloads
      - /bulk-storage/appdata/fshare-arr-bridge/pyload/plugins:/config/pyload/userplugins
    networks:
      - fshare-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000" ]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  fshare-network:
    driver: bridge
