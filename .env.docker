# Flasharr Environment Variables for Docker
# This file is intentionally minimal - most configuration is done through the web UI

# Note: Fshare credentials are configured through the web UI (Settings page)
# Note: TMDB API key is embedded in the application  
# Note: Indexer API key is configured through the web UI (Settings → Indexer)

